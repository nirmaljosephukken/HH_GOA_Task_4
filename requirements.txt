# core agent
requests>=2.31
pandas>=2.0
numpy>=1.24
# TigerGraph MCP (the agent talks to the graph through it; falls back to RESTPP if unavailable)
mcp>=1.2,<2
tigergraph-mcp>=1.0
# analyst console
streamlit>=1.35
# only needed to retrain the case-memory model (prep/train_memory_model.py)
scikit-learn>=1.3
# for running the Playwright UI smoke test (optional)
# playwright
